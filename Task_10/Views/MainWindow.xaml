<Window x:Class="Task_10.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task_10.Views"
        xmlns:viewModels="clr-namespace:Task_10.ViewModels"
        Title="Размещение деревьев"
        Width="400"
        Height="300"
        MinWidth="350" MinHeight="250"
        d:DataContext="{d:DesignData viewModels:MainWindowViewModel}"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!-- Тип деревьев -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label
        Grid.Column="0"
        Margin="0,0,10,0"
        VerticalAlignment="Center"
        Content="Тип деревьев:" />

            <ComboBox
        Grid.Column="1"
        ItemsSource="{Binding TreeTypes}"
        SelectedItem="{Binding SelectedTreeType}">

                <ComboBox.Resources>
                    <viewModels:TreeTypeConverter x:Key="TreeTypeConverter" />
                </ComboBox.Resources>

                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource TreeTypeConverter}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>

            </ComboBox>
        </Grid>

        <!-- Количество -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label
        Grid.Column="0"
        Margin="0,0,10,0"
        VerticalAlignment="Center"
        Content="Количество:" />

            <TextBox
        Grid.Column="1"
        Padding="5,2"
        VerticalContentAlignment="Center"
        Text="{Binding Count, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <!-- Статус -->
        <TextBlock
    Grid.Row="2"
    Margin="0,0,0,15"
    FontStyle="Italic"
    Foreground="Gray"
    Text="{Binding StatusMessage}"
    TextWrapping="Wrap" />

        <!-- Кнопка размещения -->
        <Button
            Grid.Row="4"
            Padding="20,8"
            Command="{Binding PlaceCommand}"
            Content="Разместить"
            FontSize="14"
            FontWeight="Bold" />
        
    </Grid>

</Window>
